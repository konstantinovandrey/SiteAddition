{
  "scripts": {
    "dev": "tinacms dev -c",
    "build": "tinacms build && quartz build"
  },
  "dependencies": {
    "tinacms": "^2.1.1"
  },
  "devDependencies": {
    "@tinacms/cli": "^0.60.5",
    "@types/node": "^22.13.14"
  }
}
